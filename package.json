{
  "name": "crunchbase-lead-finder",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "extension",
    "dashboard"
  ],
  "scripts": {
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "build:extension": "npm run build --workspace=extension",
    "build:dashboard": "npm run build --workspace=dashboard"
  }
}
